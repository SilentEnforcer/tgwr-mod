RAJ_naval_invade_basra = {
	allowed = {
		OR = {
			original_tag = RAJ
			original_tag = ENG
		}
	}
	enable = {
		has_war_with = TUR
		NOT = {
			has_global_flag = great_war_ends
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
        type = invasion_unit_request
        country_trigger = { 
			owns_state = 874			
		}
        value = 100
    }
	ai_strategy = {
		type = invasion_unit_request
		tag = TUR
		state_trigger = {
			state = 874
		}
		value = 500
	}
	ai_strategy = {
		type = front_control
		tag = TUR
		ratio = 0.1
		strategic_region = 28
		priority = 100
		ordertype = invasion
		execute_order = yes
	}
	ai_strategy = {
		type = front_unit_request

		state = 874

		value = 100
	}
}
RAJ_garisson_south_persia = {
	allowed = {
		original_tag = RAJ
	}
	enable = {
		is_subject_of = ENG
		has_military_access_to = PER
		has_war_with = TUR
		OR = {
			PER = {
				has_war_with = TUR
			}
			413 = {
				OR = {
					is_owned_by = ENG
					is_owned_by = ROOT
				}
			}	
		
		}	
			
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_unit_request

		state = 412
		state = 413
		state = 410

		value = 50
	}
}
RAJ_garisson_aden = {
	allowed = {
		original_tag = RAJ
	}
	enable = {		
		owns_state = 659
		NOT = {
			has_war_with = TUR
		}	
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = put_unit_buffers
		
		states = {
			659
		}
		ratio = 0.5
		area = middle_east
		subtract_invasions_from_need = yes
		subtract_fronts_from_need = yes
	}
}
RAJ_protect_aden = {
	allowed = {
		original_tag = RAJ
	}
	enable = {		
		owns_state = 659
		has_war_with = TUR
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_unit_request

		state = 659

		value = 50
	}
}
