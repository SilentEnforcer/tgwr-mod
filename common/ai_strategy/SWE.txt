SWE_great_war_neutrality = {
	allowed = {
		original_tag = SWE
	}
	enable = {
		has_global_flag = kis_war
		NOT = {
			has_war_with = FRA
			has_war_with = GER
			is_in_faction_with = FRA
			is_in_faction_with = GER
			has_global_flag = great_war_ends
			has_completed_focus = SWE_join_the_entente
			has_completed_focus = SWE_join_the_central_powers
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = alliance
		id = "GER"
		value = -1000
	}
	ai_strategy = {
		type = alliance
		id = "FRA"
		value = -1000
	}
}

SWE_ignore_sweden = {
	allowed = {
		OR = {
			original_tag = GER
			original_tag = FRA
		}
	}
	enable = {
		has_global_flag = kis_war
		NOT = {
			has_global_flag = great_war_ends
			has_completed_focus = GER_pressure_on_scandinavia
		}
		SWE = {
			has_completed_focus = SWE_svensk_sjalvstandigt
			NOT = {
				is_in_faction_with = ROOT
				has_war_with = ROOT
				is_subject_of = ROOT
			}
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = alliance
		id = "SWE"
		value = -1000
	}
	ai_strategy = {
		type = alliance
		id = "SWE"
		value = -1000
	}
}

SWE_wartime_preparations_mil = {
	allowed = {
		original_tag = SWE
	}
	enable = {
		has_idea = SWE_swedish_rearmament
	}
	abort = {
		NOT = { has_idea = SWE_swedish_rearmament }
		has_war = yes
	}
	
	ai_strategy = {
		type = added_military_to_civilian_factory_ratio
		value = 25
	}
	ai_strategy = {
		type = dockyard_to_military_factory_ratio
		value = 10
	}
	ai_strategy = {
		type = equipment_stockpile_surplus_ratio
		value = 10
	}
}