scripted_gui = {
	QIN_court_opp_ui = {
		context_type = player_context
		window_name = "court_opposition_ui_window"
		parent_window_token = top_bar
		
		visible = {
			tag = QIN
			NOT = { has_completed_focus = QIN_full_constitutional_monarchy }
		}
		
	    triggers = {
		    court_opposition_visible = {
				QIN = { has_idea = QIN_idea_court_opposition }
		    }
			court_opposition_not_active_visible = {
				NOT = { has_idea = QIN_idea_court_opposition }
			}
			court_opp_text_visible = {
				QIN = { has_idea = QIN_idea_court_opposition }
			}
		}
		
		ai_enabled = {
			always = no
		}
	}
}
