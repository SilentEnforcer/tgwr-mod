scripted_gui = {
	
 	USA_taft_republican_scripted_decision_ui = {
		window_name = "usa_internal_faction_scripted_gui_window"
		context_type = decision_category

		visible = {
			#has_country_flag = open_my_new_gui
			always = yes
		}

		effects = {
			#close_button_click = {
			#	clr_country_flag = open_my_new_gui
			#}
		}

		triggers = {
		}

		dynamic_lists = {
		}

		properties = {
		}

		ai_enabled = {
			always = no
		}

		ai_test_interval = 290  # The higher, the long it takes the ai to do actions
		ai_test_variance = 0.5

		ai_check = {
		}

		ai_check_scope = {
		}

		ai_weights = {
		}
	}
	USA_testing_scripted_decision_ui = {
		window_name = "usa_testing_scripted_decision_ui_window"
		context_type = decision_category

		visible = {
			#has_country_flag = open_my_new_gui
			always = no
		}

		effects = {
			#close_button_click = {
			#	clr_country_flag = open_my_new_gui
			#}
		}

		triggers = {
		}

		dynamic_lists = {
		}

		properties = {
		}

		ai_enabled = {
			always = no
		}

		ai_test_interval = 290  # The higher, the long it takes the ai to do actions
		ai_test_variance = 0.5

		ai_check = {
		}

		ai_check_scope = {
		}

		ai_weights = {
		}
	}
}