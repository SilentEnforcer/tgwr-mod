peace_deals = {
	#Sue for peace against Romania (Outside of Balkan Wars or The Great War)
	BUL_sue_for_peace_ROM = {
			icon = GFX_decision_eng_trade_unions_support
			allowed = {
				original_tag = BUL
			}
			available = {
				has_war_with = ROM
				OR = { 
					surrender_progress > 0.1
					war_support < 0.6
				}
				ROM = { is_in_faction = no }
				NOT = { 
					is_in_faction_with = GER 
					has_war_together_with = GER
					is_in_faction_with = FRA 
					has_war_together_with = FRA
				}
			}
			visible = {
				NOT = {
					is_in_faction_with = GER
					has_war_together_with = GER
					is_in_faction_with = FRA
					has_war_together_with = FRA
				}
				has_war_with = ROM
				is_subject = no
				ROM = { is_in_faction = no }
				owns_state = 77
			}
			complete_effect = { 
				ROM = {
					white_peace = BUL
					transfer_state = 77  
				}
				add_stability = -0.05
				add_war_support = -0.05
			}
	}

	#Sue for peace against the Ottomans during First Balkan War
	BUL_sue_for_peace_TUR = {
		icon = GFX_decision_eng_trade_unions_support
		allowed = {
			original_tag = BUL
		}
		available = {
			has_war_with = TUR
			OR = {
				surrender_progress > 0.1
				war_support < 0.6
			}
			NOT = {
				owns_state = 759
				owns_state = 225
				owns_state = 184
				owns_state = 788
				owns_state = 203
				owns_state = 106
				owns_state = 731
				owns_state = 345
				owns_state = 757
				owns_state = 451
				owns_state = 200
				owns_state = 849
				owns_state = 756
			}
		}
			visible = {
				NOT = {
					is_in_faction_with = GER
					has_war_together_with = GER
					is_in_faction_with = FRA
					has_war_together_with = FRA
				}
				has_war_with = TUR
				is_subject = no
				TUR = { is_in_faction = no }
				owns_state = 212
			}
			complete_effect = {
				TUR = {
					country_event = {
						id = balkan_league.20
						}
					}
				}

			}
		}

########################################################################Bulgaria Arms Purchases##########################################

BUL_arms_purchases = {
    purchase_austrian_rifles = {
        icon = GFX_decision_generic_prepare_civil_war
        cost = 75
        days_remove = 15
        days_re_enable = 100
        available = {
            has_completed_focus = BUL_authorize_arms_purchases
            has_opinion = {
                target = AUH
                value > 50
            }
        }
        visible = {
            has_completed_focus = BUL_authorize_arms_purchases
        }
        complete_effect = {
            add_equipment_to_stockpile = {
                type = infantry_equipment_1
                amount = 1000
                producer = AUH
            }
        }
    }
    
    purchase_german_rifles = {
        icon = GFX_decision_generic_prepare_civil_war
        cost = 75
        days_remove = 15
        days_re_enable = 100
        available = {
            has_completed_focus = BUL_authorize_arms_purchases
            has_opinion = {
                target = GER
                value > 50
            }
        }
        visible = {
            has_completed_focus = BUL_authorize_arms_purchases
        }
        complete_effect = {
            add_equipment_to_stockpile = {
                type = infantry_equipment_1
                amount = 1000
                producer = GER
            }
        }
    }
    
    purchase_russian_rifles = {
        icon = GFX_decision_generic_prepare_civil_war
        cost = 50
        days_remove = 15
        days_re_enable = 90
        available = {
            has_opinion = {
                target = RUS
                value > 25
            }
            has_completed_focus = BUL_authorize_arms_purchases
        }
        visible = {
            has_completed_focus = BUL_authorize_arms_purchases
        }
        complete_effect = {
            add_equipment_to_stockpile = {
                type = infantry_equipment_1
                amount = 1500
                producer = RUS
            }
        }
    }
    
    purchase_french_rifles = {
        icon = GFX_decision_generic_prepare_civil_war
        cost = 75
        days_remove = 15
        days_re_enable = 100
        available = {
            has_opinion = {
                target = FRA
                value > 50
            }
            has_completed_focus = BUL_authorize_arms_purchases
        }
        visible = {
            has_completed_focus = BUL_authorize_arms_purchases
        }
        complete_effect = {
            add_equipment_to_stockpile = {
                type = infantry_equipment_1
                amount = 1000
                producer = FRA
            }
        }
    }
}

	BUL_nationalize_foreign_industry = {
		icon = generic_civil_support
		available = {
			has_war = no
			tag = BUL
		}
		fire_only_once = yes
		cost = 150
		complete_effect = {
			hidden_effect = {
				every_country = {
					limit = {
						OR = {
							tag = AUH
							tag = BUL
						}
					}
					add_to_variable = {
						var = BUL_vassalisation
						value = -3
					}
				}
			}
			custom_effect_tooltip = BUL_vassalisation_diminution_3
			custom_effect_tooltip = back_in_120_days
		}
		ai_will_do = { 
			base = 1
		}
	}
	BUL_Complicate_investment_bureaucracy = {
		icon = generic_civil_support
		available = {
			has_war = no
			tag = BUL
		}
		fire_only_once = yes
		cost = 50
		complete_effect = {
			hidden_effect = {
				every_country = {
					limit = {
						OR = {
							tag = AUH
							tag = BUL
						}
					}
					add_to_variable = {
						var = BUL_vassalisation
						value = -1
					}
				}
			}
			custom_effect_tooltip = BUL_vassalisation_diminution
			custom_effect_tooltip = back_in_120_days
		}
		ai_will_do = { 
			base = 1
		}
	}
	BUL_counter_investments = {
		icon = generic_civil_support
		available = {
			has_war = no
			tag = BUL
		}
		
		fire_only_once = yes
		days_remove = 60
		remove_effect = {
			hidden_effect = {
				every_country = {
					limit = {
						OR = {
							tag = AUH
							tag = BUL
						}
					}
					add_to_variable = {
						var = BUL_vassalisation
						value = -1
					}
				}
			}
			custom_effect_tooltip = BUL_vassalisation_diminution
			custom_effect_tooltip = back_in_180_days
		}
		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { 
			base = 1
		}
	}
}

# political_actions = {
# 	BUL_attack_tur = {
# 		allowed = {
# 			original_tag = BUL
# 		}
# 		available = {
# 			SER = {
# 				has_war_with = TUR
# 				NOT = {
# 					is_in_faction_with = BUL
# 				}
# 			}
# 		}
# 		visible = {
# 			has_completed_focus = BUL_lone_liberator
# 		}
# 		complete_effect = {
# 			declare_war_on = {
# 				target = TUR
# 				type = take_state_focus
# 			}
# 		}
# 	}
# }

crisis = {
	BUL_march_on_sofia = {
		name = ww1_bulgaria.25.t
		allowed = {
			tag = BUL
		}
		icon = GFX_decision_revolt
		activation = {
			has_war_with = FRA
			war_length_with = {
				tag = FRA
				months > 28
			}
			OR = {
				surrender_progress > 0.50
				AND = {
					surrender_progress > 0.30
					OR = {
						has_war_support < 0.50
						has_stability < 0.50
					}
				}
				AND = {
					OR = {
						surrender_progress > 0.01
						has_war_support < 0.50
						has_stability < 0.50
					}
					OR = {
						has_global_flag = mudros_armistice
						AND = {
							has_war_with = GRE
							NOT = { controls_state = 345 }
						}
					}
					casualties > 50000
				}
				AND = {
					OR = {
						has_war_support < 0.20
						has_stability < 0.20
						surrender_progress > 0.10
					}
					casualties > 75000
				}
				casualties > 100000
			}
			NOT = { has_global_flag = march_on_sofia }
		}
		available = {
			always = no
		}
		cancel_trigger = {
			OR = {
				has_capitulated = yes
				has_war = no
				has_civil_war = yes
			}
		}
		is_good = no
		fire_only_once = no
		days_mission_timeout = 180
		timeout_effect = {
			country_event = ww1_bulgaria.25
			set_global_flag = march_on_sofia
		}
	}
}
