foreign_politics = {
	AUH_sue_for_peace_GER = {
		allowed = {
			original_tag = AUH
		}
		available = {
			has_war_with = GER
			surrender_progress > 0.7
		}
		visible = {	
			has_war_with = GER
			NOT = {
				has_global_flag = kis_war
			}
		}
		fire_only_once = yes
		ai_will_do = {
			factor = 0
			modifier = {
				OR = {
					surrender_progress > 0.9
					AND = {
						has_country_flag = galicia_released
						has_country_flag = croatia_released
					}
				}
			}
		}
		complete_effect = {
			GER = { country_event = { id = ww1_bruderkrieg.1 } }
		}
	}
}