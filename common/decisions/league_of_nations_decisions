league_of_nations_category = {
	join_league_of_nations = {
		allowed = {
		}
		visible = {
			NOT = {
				has_idea = league_of_nations_member
			}
		}
		available = {
			has_offensive_war = no
			NOT = {
				has_global_flag = world_war_2
				has_country_flag = left_from_league_of_nations
				has_country_flag = refused_by_league_of_nations
			}
		}
		cancel_trigger = {
			has_offensive_war = yes
		}
		fire_only_once = no
		days_remove = 10
		cost = 75
		remove_effect = {
			country_evet = {
				id = league_of_nations.1
			}
		}
		cancel_effect = {
			add_political_power = 75
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1000
				OR = {
					AND = {
						tag = GER
						has_government = democratic
						has_global_flag = interwar_locarno_treaties
					}
					AND = {
						tag = SOV
						OR = {
							JAP = {
								has_government = fascism
								NOT = {
									has_idea = league_of_nations_member
								}
							}
							ITA = {
								has_government = fascism
								NOT = {
									has_idea = league_of_nations_member
								}
							}
						}
					}
					AND = {
						OR = {
							tag = AUS
							tag = BUL
							tag = HUN
						}
						NOT = {
							has_idea = GER_idea_reparations
						}
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
					}
					AND = {
						OR = {
							tag = COS
							tag = FIN
							tag = LUX
							tag = ALB
						}
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
						any_country = {
							OR = {
								tag = AUS
								tag = BUL
							}
							has_idea = league_of_nations_member
						}
					}
					AND = {
						OR = {
							tag = EST
							tag = LAT
							tag = LIT
						}
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
						POL = {
							has_war = no
						}
						SOV = {
							has_war = no
						}
					}
					AND = {
						tag = IRE
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						OR = {
							is_subject_of = ENG
							is_subject = no
						}
					}
					AND = {
						tag = ETH
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						NOT = {
							has_country_flag = great_war_loser
						}
						is_subject = no
						owns_state = 657 #Ogaden
					}
					AND = {
						OR = {
							tag = DOM
							tag = IRQ
							tag = AFG
							tag = EGY
						}
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
					}
					AND = {
						tag = MEX
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						NOT = {
							has_country_flag = great_war_loser
						}
						has_global_flag = mexican_revolution
						is_subject = no
					}
					AND = {
						tag = TUR
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						has_country_flag = TUR_national_movement_tag
						is_subject = no
						owns_state = 49 #Ankara
					}
					AND = {
						tag = ECU
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
						SOV = {
							has_idea = league_of_nations_member
						}
					}
					AND = {
						tag = ARG
						OR = {
							has_government = democratic
							has_government = neutrality
						}
						is_subject = no
						date > 1933.9.1
					}
				}
			}
		}
	}
	leave_league_of_nations = {
		allowed = {
			always = yes
		}
		visible = {
			has_idea = league_of_nations_member
		}
		available = {
		}
		fire_only_once = no
		days_remove = 10
		cost = 0
		remove_effect = {
			remove_ideas = league_of_nations_member
			set_country_flag = left_from_league_of_nations
			hidden_effect = {
				country_event = {
					id = league_of_nations.8
					days = 365
				}
			}
			every_other_country = {
				limit = {
					has_idea = league_of_nations_member
					has_non_aggression_pact_with = PREV
					NOT = {
						is_in_faction_with = PREV
						has_guaranteed = PREV 
					}
				}
				diplomatic_relation = {
					country = PREV
					relation = non_aggression_pact
					active = no
				}
				news_event = {
					id = league_of_nations.7
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1000
				OR = {
					AND = {
						is_subject = yes
						OVERLORD = {
							NOT = {
								has_idea = league_of_nations_member
							}
						}
					}
					AND = {
						tag = ARG
						date > 1921.1.10
						date < 1933.1.1
					}
					AND = {
						tag = BRA
						date > 1926.6.14
					}
					AND = {
						tag = JAP
						MAN = {
							is_subject_of = JAP
						}
					}
					AND = {
						tag = COS
						date > 1925.1.22
					}
					AND = {
						tag = GER
						has_government = fascism
					}
					AND = {
						is_major = no
						has_offensive_war = yes
					}
				}
			}
		}
	}
	# WORK IN PROCESS
	#embargo_threatening_country = {
	#
	#}
	#disfunction_with_ww2 = {
	#
	#}
}
